{
  "name": "polkatax",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "rimraf server/public && cd client && npm run build && cp -r ./dist/spa  ../server/public"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "rimraf": "^5.0.1"
  }
}
